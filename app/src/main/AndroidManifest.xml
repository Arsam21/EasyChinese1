<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mad.easychinese">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".stroke" />
        <activity android:name=".lesson" />
        <activity android:name=".animalContent" />
        <activity android:name=".game" />
        <activity android:name=".result" />
        <activity android:name=".home" />
        <activity android:name=".lesson_animal" />

        <service
            android:name=".MyAlarmService"
            android:enabled="true" />

        <receiver android:name=".MyReceiver" />

        <activity android:name=".reminder" />
        <activity android:name=".gameMenu" />
        <activity android:name=".fruitContent" />
        <activity android:name=".schoolContent" />
        <activity android:name=".plantContent"></activity>
    </application>

</manifest>